const force = (mass) => ({
  d = { // Displacement
    x: 0,
    y: 0,
    z: 0,
  };
  f = { // Force
    x: 0,
    y: 0,
    z: 0,
  };
  v = { // Velocity
    x: 0,
    y: 0,
    z: 0,
  };
  a = { // Acceleration
    x: 0,
    y: 0,
    z: 0,
  };
  mass = mass;
})